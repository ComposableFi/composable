import BrowserOnly from '@docusaurus/BrowserOnly';
import {VmWrapper} from '../../src/components/vm/VmWrapper';
import {ResetButton} from '../../src/components/vm/ResetButton'; import {
	unhexifyKeys
} from '../../src/utils/cosmwasm-vm/parsers';

# SCDI test md file

## SCDI test

<br />

<div className='max-w-[600px] overflow-auto'>
	<ResetButton />
	<VmWrapper storageId='test' initOptions={{
		contractUrl: '/wasm/cw20_base.wasm',
		instantiateObj: {
			"name": "picasso",
			"symbol": "PICA",
			"decimals": 12,
			"initial_balances": [],
			"mint": {
				"minter": "0",
				"cap": null
			},
			"marketing": null
		}
	}}>
		{(vmShared) => {
			if(!vmShared) return null;
			return (
				<>
					<p>nextAccountId</p>
					<pre>{vmShared.nextAccountId}</pre>
				</>
			)
		}}
	</VmWrapper>
	text text text
	<VmWrapper storageId={'test'}>
		{(vmShared) => {
			if(!vmShared) return <span>loading...</span>;
			const storageObj = unhexifyKeys(Object.fromEntries(vmShared.storage.get('10000').entries()));
			return (
				<span className={'mx-5 bg-gray-600'}>{JSON.stringify(storageObj)}</span>
			)
		}}
	</VmWrapper>
	text text text
</div>