//! Helps connecting identifiers into networks.
//! Allows to map asset identifiers, networks, channels, denominations from, to and on each chain via storage, precompiles, host extensions.  

pub fn get_route(network_id: xc_core::NetworkId, asset: AssetId) -> (String, String) {
    todo!()
}