
install:
	yarn install

build:
	yarn build

build-types:
	cd packages/types && yarn gen && yarn build

build-utils:
	cd packages/utils && yarn build

build-oracle_setup:
	cd packages/oracle_setup && yarn build

build-oracle_setup:
	cd packages/bootstrap_pallets && yarn build

lint:
	yarn lint

oracle_setup:
	cd packages/oracle_setup && yarn install && yarn start
