name: stats

on:
  pull_request:
    types: [opened]

jobs:
  pull-request-stats:
    runs-on: ubuntu-latest
    steps:
      - name: pull-request-stats
        uses: flowwer-dev/pull-request-stats@master