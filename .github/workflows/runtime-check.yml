name: Release Drafter
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - dali-chachacha
      - picasso
jobs:
  update_release_draft:
    runs-on: ubuntu-latest
    steps:
      - uses: Check for runtime changes
        run: |
          sh ../.maintain/check_runtime.sh