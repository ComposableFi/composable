name: Release Drafter
on:
  push:
    branches:
      - main
      - check_runtime_ci
  pull_request:
    branches:
      - dali-chachacha
      - picasso
jobs:
  update_release_draft:
    runs-on: ubuntu-latest
    steps:
      - name: Check for runtime changes
        run: |
          sh ../.maintain/check_runtime.sh