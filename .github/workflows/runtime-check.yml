name: Check Runtime
on:
  push:
    branches:
      - main
      - check_runtime_ci
  pull_request:
    branches:
      - dali-chachacha
      - picasso
jobs:
  check-runtime:
    runs-on: ubuntu-latest
    steps:
      - name: Check for runtime changes
        run: |
          ls
          pwd
          sh ../.maintain/check_runtime.sh