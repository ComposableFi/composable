[package]
authors = ["Composable Developers"]
edition = "2021"
homepage = "https://composable.finance"
name = "xcvm-protocols"
version = "0.1.0"

[dependencies]
ethabi = { version = "17.0.0", default-features = false }
xcvm-core = { path = "../core", default-features = false }

[features]
default = []
std = ["xcvm-core/std"]
