To run:

1. build polkadot

	```bash
	git clone https://github.com/paritytech/polkadot
	cd polkadot
	git checkout v0.9.9-rc2
	cargo build --release
    ```

2. build this repo

	```bash
	yarn
	yarn dev
	```
