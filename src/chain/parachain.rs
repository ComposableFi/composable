pub struct ParachainClient {
    // hold relay chain and parachain clients

    // track light client on the counterparty chain
}

pub struct ParachainClientConfig {
    // config options for [`ParachainClient`]

}