version: "3.9"

services:
  composable:
    image: composablefi/devnet-container:latest
    ports:
      - "9944:9944"
      - "9988:9988"
  integration-tests:
    image: node:bullseye
    command: ./dockerRun.sh
    working_dir: /code
    volumes:
      - .:/code
    environment:
      - ENDPOINT=composable:9988
    depends_on:
      - composable
